import { toast } from "react-toastify";

export const errorToastGlobel = () => { return toast.error("fdsfdfsd") };
export const successToastGlobel = () => { return toast.success("fdsfdfsd") };
export const warnToastGlobel = () => { return toast.warn("fdsfdfsd") };

export const errorToast = (mes) => {return toast.error(mes)}
export const successToast = (mes) => {return toast.success(mes)}
export const warnToass = (mes) => {return toast.warn(mes)}